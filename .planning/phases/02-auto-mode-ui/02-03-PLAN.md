---
phase: 02-auto-mode-ui
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/arena/ConversationStream.tsx
  - src/components/arena/ArenaBubble.tsx
  - src/components/arena/ArenaStatusBar.tsx
  - src/components/arena/GradeReport.tsx
  - src/components/arena/ArenaView.tsx
autonomous: false
requirements:
  - UI-02
  - UI-03

must_haves:
  truths:
    - "运行中 Gatekeeper 消息靠左显示蓝色气泡，Challenger 消息靠右显示 primary 色气泡"
    - "Challenger 流式输出逐 token 追加到气泡中，末尾有闪烁光标"
    - "对话区顶部固定状态栏显示当前轮次、运行状态和 token 用量"
    - "新消息出现时自动滚动到底部，手动上滑暂停自动滚动"
    - "运行完成后评分报告在对话流下方展开，显示通关状态、必须项、表现项和改进建议"
    - "状态栏有 Stop 按钮可中断运行"
  artifacts:
    - path: "src/components/arena/ConversationStream.tsx"
      provides: "对话流滚动容器"
      exports: ["ConversationStream"]
    - path: "src/components/arena/ArenaBubble.tsx"
      provides: "消息气泡组件"
      exports: ["ArenaBubble"]
    - path: "src/components/arena/ArenaStatusBar.tsx"
      provides: "顶部状态栏"
      exports: ["ArenaStatusBar"]
    - path: "src/components/arena/GradeReport.tsx"
      provides: "评分报告组件"
      exports: ["GradeReport"]
  key_links:
    - from: "src/components/arena/ArenaView.tsx"
      to: "src/components/arena/ConversationStream.tsx"
      via: "phase running/completed conditional render"
      pattern: "ConversationStream"
    - from: "src/components/arena/ConversationStream.tsx"
      to: "src/components/arena/ArenaBubble.tsx"
      via: "messages.map render"
      pattern: "ArenaBubble"
    - from: "src/components/arena/ConversationStream.tsx"
      to: "src/components/arena/GradeReport.tsx"
      via: "grade conditional render below messages"
      pattern: "GradeReport"
    - from: "src/components/arena/ConversationStream.tsx"
      to: "use-stick-to-bottom"
      via: "StickToBottom wrapper"
      pattern: "StickToBottom"
---

<objective>
对话流 + 评分报告：实现实时流式对话展示（气泡布局、流式输出、自动滚动）、状态栏和评分报告组件，完成 Arena 完整端到端体验。

Purpose: 用户能看到 Gatekeeper-Challenger 对话实时流入，运行结束后查看评分报告。这是 Phase 2 的核心交互层。
Output: ConversationStream 对话流容器、ArenaBubble 消息气泡、ArenaStatusBar 状态栏、GradeReport 评分报告、ArenaView 完整集成。
</objective>

<execution_context>
@/home/user_demo/Husky/arena/.claude/get-shit-done/workflows/execute-plan.md
@/home/user_demo/Husky/arena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auto-mode-ui/02-CONTEXT.md
@.planning/phases/02-auto-mode-ui/02-RESEARCH.md
@.planning/phases/02-auto-mode-ui/02-UI-SPEC.md

<!-- Plan 01 + 02 产出 -->
@src/components/arena/types.ts
@src/hooks/useArenaSSE.ts
@src/components/arena/ArenaView.tsx

<interfaces>
<!-- Plan 01 创建的类型 (src/components/arena/types.ts) -->
```typescript
export interface ArenaUIMessage {
  id: string;
  role: 'gatekeeper' | 'challenger';
  content: string;
  turn: number;
  isStreaming?: boolean;
}
export type ArenaRunUIStatus = 'idle' | 'running' | 'grading' | 'completed' | 'error';
```

<!-- useArenaSSE hook 返回值 (src/hooks/useArenaSSE.ts) -->
```typescript
// 返回:
{
  messages: ArenaUIMessage[];        // 已完成的对话消息
  streamingDelta: string;            // 当前 Challenger 流式输出内容
  currentTurn: number;               // 当前轮次
  status: ArenaRunUIStatus;          // 运行状态
  grade: GraderOutput | null;        // 评分结果
  tokenUsage: { totalUsed: number; remaining: number } | null;
  runId: string | null;
  error: string | null;
  startRun: (params: RunParams) => Promise<void>;
  cancelRun: () => void;
}
```

<!-- GraderOutput (src/arena/schemas/grader-output.ts) -->
```typescript
export type GraderOutput = {
  passed: boolean;
  requiredCriteria: Array<{ name: string; passed: boolean; reason: string }>;
  performanceDimensions: Array<{ name: string; grade: 'A'|'B'|'C'|'D'; reason: string }>;
  suggestions: Array<{ content: string; referenceTurn: number }>;
};
```

<!-- Conversation component 模式 (src/components/ai-elements/conversation.tsx) -->
```typescript
import { StickToBottom, useStickToBottomContext } from 'use-stick-to-bottom';
// <StickToBottom className="relative flex-1 overflow-y-hidden" initial="smooth" resize="instant">
//   <StickToBottom.Content className="flex flex-col gap-4 p-4">
//     {children}
//   </StickToBottom.Content>
// </StickToBottom>
// ConversationScrollButton: 使用 useStickToBottomContext().isAtBottom 判断是否显示
```

<!-- motion/react 动画模式 -->
```typescript
import { AnimatePresence, motion } from 'motion/react';
// fadeIn + translateY: initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.2 }}
```

<!-- Icon 导出 (src/components/ui/icon.tsx) -->
```typescript
// Shield (Gatekeeper icon), Lightning (Challenger icon), Stop (stop button)
// CheckCircle (passed criteria), XCircle (failed criteria), ArrowLeft (back button)
// Info (token info), CaretDown (collapsible)
```

<!-- 颜色规范 (UI-SPEC Color section) -->
// Gatekeeper bubble: bg-muted, icon bg-blue-500/10 text-blue-500
// Challenger bubble: bg-primary text-primary-foreground, icon bg-orange-500/10 text-orange-500
// Pass banner: bg-status-success-muted border-status-success-border
// Fail banner: bg-status-error-muted border-status-error-border
// Grade A: bg-status-success-muted text-status-success-foreground
// Grade B: bg-primary/10 text-primary
// Grade C: bg-status-warning-muted text-status-warning-foreground
// Grade D: bg-status-error-muted text-status-error-foreground
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ArenaBubble + ArenaStatusBar + ConversationStream 组件</name>
  <files>src/components/arena/ArenaBubble.tsx, src/components/arena/ArenaStatusBar.tsx, src/components/arena/ConversationStream.tsx</files>
  <read_first>
    - src/components/arena/types.ts (ArenaUIMessage, ArenaRunUIStatus)
    - src/components/ai-elements/conversation.tsx (StickToBottom 用法 -- 完整复用模式)
    - src/hooks/useArenaSSE.ts (hook 返回值结构)
    - src/components/ui/icon.tsx (确认 Shield, Lightning, Stop, ArrowLeft 图标导出)
    - src/components/ui/badge.tsx (Badge 组件 API)
    - src/components/ui/button.tsx (Button 组件 API)
    - src/components/ui/tooltip.tsx (Tooltip 组件 API)
    - .planning/phases/02-auto-mode-ui/02-UI-SPEC.md (Visual Focal Points, Animation Contracts, Accessibility)
  </read_first>
  <action>
1. 创建 `src/components/arena/ArenaBubble.tsx` (D-01, D-04: 气泡布局 + 角色颜色图标)：

```typescript
'use client';
import { memo } from 'react';
import { motion } from 'motion/react';
import { Shield, Lightning } from '@/components/ui/icon';
import { cn } from '@/lib/utils';
import type { ArenaUIMessage } from './types';

interface ArenaBubbleProps {
  message: ArenaUIMessage;
  isStreaming?: boolean;
}

export const ArenaBubble = memo(function ArenaBubble({ message, isStreaming }: ArenaBubbleProps) {
  const isGatekeeper = message.role === 'gatekeeper';

  return (
    <motion.div
      initial={{ opacity: 0, y: 8 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.2, ease: 'easeOut' }}
      className={cn(
        "flex gap-2 max-w-[80%]",
        isGatekeeper ? "self-start" : "self-end flex-row-reverse"
      )}
      aria-label={isGatekeeper ? 'Gatekeeper message' : 'Challenger message'}
    >
      {/* 角色图标 (D-04) */}
      <div className={cn(
        "shrink-0 h-8 w-8 rounded-full flex items-center justify-center",
        isGatekeeper ? "bg-blue-500/10 text-blue-500" : "bg-orange-500/10 text-orange-500"
        // lint-allow-raw-color: Arena role differentiation requires specific blue/orange pairing per UI-SPEC
      )}>
        {isGatekeeper ? <Shield size={16} /> : <Lightning size={16} />}
      </div>
      {/* 消息气泡 (D-01) */}
      <div
        className={cn(
          "rounded-2xl px-4 py-3 text-sm",
          isGatekeeper
            ? "bg-muted text-foreground rounded-tl-sm"
            : "bg-primary text-primary-foreground rounded-tr-sm",
        )}
        aria-busy={isStreaming}
      >
        {message.content}
        {isStreaming && (
          <span className="inline-block w-[2px] h-[14px] ml-0.5 bg-current animate-pulse align-text-bottom" />
        )}
      </div>
    </motion.div>
  );
});
```

关键细节：
- React.memo 隔离已完成消息，避免 streaming delta 变更导致全部重渲染（Pitfall 5）
- motion.div 实现入场动画：opacity 0->1, translateY 8px->0, 200ms easeOut（UI-SPEC Animation Contracts）
- Gatekeeper: `self-start` 靠左, `bg-muted`, `bg-blue-500/10 text-blue-500`, Shield 图标
- Challenger: `self-end flex-row-reverse` 靠右, `bg-primary text-primary-foreground`, `bg-orange-500/10 text-orange-500`, Lightning 图标
- 流式光标: `animate-pulse` 竖线 2px 宽 14px 高（UI-SPEC 800ms 脉冲）
- aria-label 标注角色名；aria-busy=true 标注流式状态（Accessibility Contracts）
- 气泡尖角效果: Gatekeeper `rounded-tl-sm`, Challenger `rounded-tr-sm`
- `// lint-allow-raw-color` 注释标注 blue/orange 原始色使用理由

2. 创建 `src/components/arena/ArenaStatusBar.tsx` (D-03: 顶部状态栏)：

Props: `{ currentTurn: number; maxTurns: number; status: ArenaRunUIStatus; tokenCount: number; onStop: () => void }`

- 容器: `sticky top-0 z-10 bg-background/80 backdrop-blur-sm border-b`
- 内容行: `flex items-center gap-4 px-4 py-2 text-xs font-semibold`
- 左侧: `Turn {current}/{max}` 使用 `t('arena.status.turn')` 翻译，替换 `{current}` 和 `{max}` 占位符
- 中间: 状态 Badge，根据 status 显示不同颜色和文本
  - running: variant="secondary", text = `t('arena.status.running')`
  - grading: variant="secondary", text = `t('arena.status.grading')`
  - completed: variant="default", text = `t('arena.status.completed')`
  - error: variant="destructive", text = `t('arena.status.failed')`
- 右侧: token 计数 `{count} tokens` + Stop 按钮
- Stop 按钮: 只在 status === 'running' 时显示，`variant="outline" size="sm"` 32px 高度，Stop 图标 16px
- Tooltip 包裹 Stop 按钮，content = `t('arena.stop')`
- aria-live="polite" 用于状态变更播报

3. 创建 `src/components/arena/ConversationStream.tsx` (D-01, D-05: 对话流容器 + 自动滚动)：

Props: `{ messages: ArenaUIMessage[]; streamingDelta: string; currentTurn: number; maxTurns: number; status: ArenaRunUIStatus; tokenCount: number; grade: GraderOutput | null; error: string | null; onStop: () => void; onBack: () => void }`

结构:
```
<div className="flex h-full flex-col">
  <ArenaStatusBar currentTurn={currentTurn} maxTurns={maxTurns} status={status} tokenCount={tokenCount} onStop={onStop} />
  <StickToBottom className="relative flex-1 overflow-y-hidden" initial="smooth" resize="instant">
    <StickToBottom.Content className="flex flex-col gap-4 p-4">
      <AnimatePresence mode="popLayout">
        {messages.map(msg => <ArenaBubble key={msg.id} message={msg} />)}
        {streamingDelta && (
          <ArenaBubble
            key="streaming"
            message={{ id: 'streaming', role: 'challenger', content: streamingDelta, turn: currentTurn }}
            isStreaming
          />
        )}
      </AnimatePresence>
      {grade && <GradeReport grade={grade} />}
      {error && status === 'error' && (
        <div className="text-center text-sm text-destructive py-4">
          {t('arena.error.runFailed' as TranslationKey).replace('{reason}', error)}
        </div>
      )}
    </StickToBottom.Content>
    <ScrollButton />
  </StickToBottom>
  {/* 底部返回按钮 -- 仅在 completed/error 状态显示 */}
  {(status === 'completed' || status === 'error') && (
    <div className="shrink-0 border-t px-4 py-3">
      <Button variant="ghost" onClick={onBack}>
        <ArrowLeft size={16} className="mr-2" />
        {t('arena.backToLevels' as TranslationKey)}
      </Button>
    </div>
  )}
</div>
```

ScrollButton 组件（内部定义）:
- 使用 `useStickToBottomContext()` 获取 `isAtBottom` 和 `scrollToBottom`
- 不在底部时显示一个 `ArrowDown` 按钮（复用 conversation.tsx 中 ConversationScrollButton 的模式）
- 位置: `absolute bottom-4 left-[50%] translate-x-[-50%]`

关键注意事项:
- 使用 `StickToBottom` from `use-stick-to-bottom` 实现自动滚动（D-05）
- AnimatePresence 包裹消息列表实现入场动画
- streaming 气泡使用固定 key="streaming" 避免重复创建
- GradeReport 在 StickToBottom.Content 内部、最后一条消息之后渲染（D-09: 不跳转页面）
- GradeReport 上方 mt-12 (48px) 间距（UI-SPEC spacing）
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/arena && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/arena/ArenaBubble.tsx 存在且导出 `ArenaBubble`
    - ArenaBubble.tsx 包含 `memo(function ArenaBubble` (React.memo 优化)
    - ArenaBubble.tsx 包含 `bg-muted` (Gatekeeper 气泡色) 和 `bg-primary` (Challenger 气泡色)
    - ArenaBubble.tsx 包含 `bg-blue-500/10` 和 `bg-orange-500/10` (角色图标背景)
    - ArenaBubble.tsx 包含 `Shield` 和 `Lightning` 图标导入
    - ArenaBubble.tsx 包含 `self-start` (左对齐) 和 `self-end` (右对齐)
    - ArenaBubble.tsx 包含 `animate-pulse` (流式光标)
    - ArenaBubble.tsx 包含 `aria-busy` 和 `aria-label` (可访问性)
    - src/components/arena/ArenaStatusBar.tsx 存在且导出 `ArenaStatusBar`
    - ArenaStatusBar.tsx 包含 `sticky top-0 z-10`
    - ArenaStatusBar.tsx 包含 `backdrop-blur-sm`
    - ArenaStatusBar.tsx 包含 `Stop` 图标导入
    - ArenaStatusBar.tsx 包含 `aria-live`
    - src/components/arena/ConversationStream.tsx 存在且导出 `ConversationStream`
    - ConversationStream.tsx 包含 `StickToBottom` 导入 from `use-stick-to-bottom`
    - ConversationStream.tsx 包含 `AnimatePresence` 导入
    - ConversationStream.tsx 包含 `ArenaBubble` 和 `ArenaStatusBar` 和 `GradeReport` 导入
    - ConversationStream.tsx 包含 `key="streaming"` (流式气泡固定 key)
    - TypeScript 类型检查通过
  </acceptance_criteria>
  <done>ArenaBubble 左右分布显示角色消息；ArenaStatusBar 固定顶部显示状态；ConversationStream 使用 StickToBottom 自动滚动并渲染完整对话流 + 评分报告</done>
</task>

<task type="auto">
  <name>Task 2: GradeReport 组件 + ArenaView 完整集成</name>
  <files>src/components/arena/GradeReport.tsx, src/components/arena/ArenaView.tsx</files>
  <read_first>
    - src/arena/schemas/grader-output.ts (GraderOutput 类型 -- 评分数据结构)
    - src/components/arena/types.ts (ArenaUIMessage, ArenaRunUIStatus)
    - src/components/arena/ArenaView.tsx (当前 running/completed 占位 -- 需要替换为 ConversationStream)
    - src/components/arena/ConversationStream.tsx (Task 1 刚创建)
    - src/hooks/useArenaSSE.ts (hook 返回值)
    - src/components/ui/badge.tsx (Badge 组件 API)
    - src/components/ui/card.tsx (Card 组件 API)
    - src/components/ui/separator.tsx (Separator 组件 API)
    - .planning/phases/02-auto-mode-ui/02-UI-SPEC.md (Grade Report Layout Specifications, Color)
  </read_first>
  <action>
1. 创建 `src/components/arena/GradeReport.tsx` (D-09, UI-03: 评分报告)：

Props: `{ grade: GraderOutput }`

```typescript
'use client';
import { motion } from 'motion/react';
import { CheckCircle, XCircle } from '@/components/ui/icon';
import { Badge } from '@/components/ui/badge';
import { Card, CardContent } from '@/components/ui/card';
import { Separator } from '@/components/ui/separator';
import { useTranslation } from '@/hooks/useTranslation';
import type { TranslationKey } from '@/i18n';
import type { GraderOutput } from '@/arena/schemas/grader-output';
import { cn } from '@/lib/utils';
```

布局结构（top to bottom, from UI-SPEC）：

**整体容器:**
- `mt-12` (48px top margin, from UI-SPEC 2xl spacing)
- `border rounded-xl bg-card p-6`
- motion.div 入场动画: `initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.3, ease: 'easeOut' }}`

**Section 1: Pass/Fail Banner**
- 全宽 `rounded-lg p-4`
- 通过: `bg-status-success-muted border border-status-success-border`
- 未通过: `bg-status-error-muted border border-status-error-border`
- 标题: `text-xl font-semibold` + `t('arena.grade.passed')` 或 `t('arena.grade.failed')`
- Badge: 通过用 `variant="default"` (`bg-primary`), 未通过用 `variant="destructive"`
- `aria-live="assertive"` 让屏幕阅读器立即播报结果

**Section 2: Required Criteria** (gap-6 between sections)
- 标题: `text-base font-semibold` `t('arena.grade.requiredCriteria')`
- 每个 criterion 一行: `flex items-start gap-3`
  - 图标: `CheckCircle` (passed, `text-status-success`) 或 `XCircle` (failed, `text-status-error`), size=16
  - 名称: `font-semibold text-sm`
  - 理由: `text-sm text-muted-foreground`

**Section 3: Performance Dimensions** (Separator 分隔)
- 标题: `text-base font-semibold` `t('arena.grade.performanceDimensions')`
- 每个 dimension 一行: `flex items-start gap-3`
  - Badge 显示等级字母，颜色对照表：
    - A: `className="bg-status-success-muted text-status-success-foreground"`
    - B: `className="bg-primary/10 text-primary"`
    - C: `className="bg-status-warning-muted text-status-warning-foreground"`
    - D: `className="bg-status-error-muted text-status-error-foreground"`
  - 名称: `font-semibold text-sm`
  - 理由: `text-sm text-muted-foreground`

**Section 4: Improvement Suggestions** (Separator 分隔)
- 标题: `text-base font-semibold` `t('arena.grade.suggestions')`
- 最多 3 条，每条用 Card 包裹: `<Card className="bg-muted/50">`
  - 建议内容: `text-sm`
  - 轮次引用: `text-xs text-muted-foreground` `t('arena.grade.turnReference').replace('{n}', String(s.referenceTurn))`

gradeBadgeClass 映射函数:
```typescript
function getGradeBadgeClass(grade: 'A' | 'B' | 'C' | 'D'): string {
  switch (grade) {
    case 'A': return 'bg-status-success-muted text-status-success-foreground';
    case 'B': return 'bg-primary/10 text-primary';
    case 'C': return 'bg-status-warning-muted text-status-warning-foreground';
    case 'D': return 'bg-status-error-muted text-status-error-foreground';
  }
}
```

2. 更新 `src/components/arena/ArenaView.tsx`，将 running/completed 占位替换为 ConversationStream：

在 switch 的 `case 'running':` 和 `case 'completed':` 分支中，将占位 div 替换为:

```typescript
case 'running':
case 'completed':
  return (
    <div className="flex h-full flex-col">
      <ConversationStream
        messages={arenaSSE.messages}
        streamingDelta={arenaSSE.streamingDelta}
        currentTurn={arenaSSE.currentTurn}
        maxTurns={selectedLevel?.maxTurns ?? 10}
        status={arenaSSE.status}
        tokenCount={arenaSSE.tokenUsage?.totalUsed ?? 0}
        grade={arenaSSE.grade}
        error={arenaSSE.error}
        onStop={arenaSSE.cancelRun}
        onBack={handleBack}
      />
    </div>
  );
```

同时需要更新 ArenaView 中的以下内容：
- 添加 `import { ConversationStream } from './ConversationStream';`
- 在 selectedLevel 状态中添加 maxTurns 字段
- 确保 handleSelectLevel 能获取 level 的 maxTurns（从 LevelCardList 传递）
- 更新 LevelCardList 的 onSelectLevel 回调签名，传递完整的 level info（包含 name, description, maxTurns）

更新 LevelCardList 组件（如果需要）：
- onSelectLevel 回调需要传递 levelName, levelDescription, maxTurns 等信息
- 修改 LevelCardList 的 onSelectLevel 类型为 `(worldId: string, levelId: string, levelInfo: { name: string; description: string; maxTurns: number }) => void`

更新 ArenaView 中 handleSelectLevel 以保存完整 level info：
```typescript
const handleSelectLevel = useCallback((worldId: string, levelId: string, levelInfo: { name: string; description: string; maxTurns: number }) => {
  setSelectedLevel({ worldId, levelId, name: levelInfo.name, description: levelInfo.description, maxTurns: levelInfo.maxTurns });
  setShowRunControls(true);
}, []);
```
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/arena && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/arena/GradeReport.tsx 存在且导出 `GradeReport`
    - GradeReport.tsx 包含 `import type { GraderOutput }` from grader-output
    - GradeReport.tsx 包含 `bg-status-success-muted` 和 `bg-status-error-muted` (pass/fail banner)
    - GradeReport.tsx 包含 `CheckCircle` 和 `XCircle` 图标
    - GradeReport.tsx 包含 `arena.grade.passed` 和 `arena.grade.failed` 翻译 key
    - GradeReport.tsx 包含 `arena.grade.requiredCriteria` 翻译 key
    - GradeReport.tsx 包含 `arena.grade.performanceDimensions` 翻译 key
    - GradeReport.tsx 包含 `arena.grade.suggestions` 翻译 key
    - GradeReport.tsx 包含 `getGradeBadgeClass` 或等效的等级颜色映射
    - GradeReport.tsx 包含 `arena.grade.turnReference` 翻译 key
    - GradeReport.tsx 包含 `motion.div` 入场动画 (duration: 0.3)
    - GradeReport.tsx 包含 `mt-12` 上间距
    - GradeReport.tsx 包含 `aria-live="assertive"` 用于 pass/fail banner
    - ArenaView.tsx 包含 `import { ConversationStream }` (不再有占位 div)
    - ArenaView.tsx 的 running/completed 分支渲染 `<ConversationStream`
    - TypeScript 类型检查通过
  </acceptance_criteria>
  <done>GradeReport 展示通关/未通关 banner、必须项逐条 Pass/Fail、表现项等级 Badge、改进建议卡片；ArenaView 在 running/completed 态渲染完整 ConversationStream + GradeReport</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Arena 端到端视觉验证</name>
  <files>src/components/arena/ArenaView.tsx</files>
  <action>
人工验证 Arena 完整端到端体验。启动 dev server 并按照 how-to-verify 步骤逐项检查。

验证项目：
1. 侧边栏 Arena 入口位置和图标
2. 关卡卡片列表渲染
3. RunControls 高级选项
4. 对话流气泡布局和流式输出
5. 状态栏信息展示
6. 评分报告内容
7. 中英文切换
  </action>
  <what-built>完整的 Arena Auto Mode UI：侧边栏入口、关卡列表、运行启动（含高级选项）、对话流（气泡布局 + 流式输出 + 自动滚动）、状态栏、评分报告</what-built>
  <how-to-verify>
    1. 启动应用: `npm run dev`
    2. 打开 http://localhost:3000
    3. 确认侧边栏导航中 Skills 后面出现 "Arena" 入口（GameController 图标）
    4. 点击 Arena，确认进入 /arena 页面，显示关卡卡片列表
    5. 确认关卡卡片有名称、简述和"开始挑战"按钮
    6. 点击"开始挑战"，确认显示 RunControls（关卡信息 + 高级选项折叠面板）
    7. 展开"高级选项"，确认显示 Gatekeeper/Challenger/Grader 三角色 provider/model 选择器
    8. 点击"开始挑战"启动运行，确认：
       - 顶部状态栏显示轮次、运行状态、token 数
       - Gatekeeper 消息在左侧蓝色气泡中出现
       - Challenger 消息在右侧带流式光标的气泡中逐 token 出现
       - 新消息出现时自动滚动到底部
    9. 运行完成后，确认评分报告在对话下方展开：
       - 通关/未通关 banner
       - 必须项 Pass/Fail 列表
       - 表现项等级 Badge
       - 改进建议卡片
    10. 切换到中文界面，确认所有文本显示中文
    11. 点击"返回关卡列表"确认回到关卡选择
  </how-to-verify>
  <verify>User confirms all 11 verification steps pass</verify>
  <done>用户确认 Arena 端到端流程视觉验证通过</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream data -> UI rendering | GradeReport 和 ArenaBubble 渲染服务端返回的文本内容 |
| User click -> cancelRun | Stop 按钮触发 AbortController.abort() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Injection | ArenaBubble content rendering | mitigate | 消息 content 通过 React JSX 文本节点渲染（非 dangerouslySetInnerHTML），React 自动转义 HTML/XSS |
| T-02-09 | Injection | GradeReport content rendering | mitigate | GraderOutput 中的 name/reason/content 通过 React JSX 文本节点渲染，自动转义 |
| T-02-10 | Denial of Service | AnimatePresence re-renders | accept | 对话消息数量有 maxTurns 上限（通常 10-20 轮），不会产生大量 DOM 节点 |
| T-02-11 | Denial of Service | streamingDelta rapid updates | mitigate | ArenaBubble 使用 React.memo 隔离已完成消息，只有 streaming 气泡重渲染 |
</threat_model>

<verification>
1. `npx tsc --noEmit` 通过
2. `npm run test` 通过
3. 视觉验证：Arena 页面完整端到端流程可用
4. grep 验证所有组件文件存在且导出正确
</verification>

<success_criteria>
- 对话流实时显示 Gatekeeper 和 Challenger 消息
- Challenger 消息逐 token 流入（D-02）
- 气泡左右分布，颜色图标正确（D-01, D-04）
- 状态栏固定顶部（D-03）
- 自动滚动到底部（D-05）
- 评分报告在对话下方展开（D-09）
- 评分报告包含 pass/fail、必须项、表现项、改进建议（UI-03）
- 视觉验证通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-auto-mode-ui/02-03-SUMMARY.md`
</output>
