---
phase: 03-world-navigation-progression
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/arena/db.ts
  - src/app/api/arena/progress/route.ts
  - src/components/arena/LevelDetail.tsx
  - src/components/arena/ArenaView.tsx
  - src/components/arena/types.ts
  - src/i18n/en.ts
  - src/i18n/zh.ts
autonomous: true
requirements: [LEVL-04, UI-05]

must_haves:
  truths:
    - "GET /api/arena/progress 返回每个 level_id 的 cleared 和 runCount"
    - "每个世界的第一关默认解锁，后续关卡需前一关通关后才解锁"
    - "锁定的关卡不可开始运行（开始按钮禁用）"
    - "关卡详情页显示场景描述、评分维度说明、开始按钮"
    - "已通关的关卡可重复挑战"
  artifacts:
    - path: "src/arena/db.ts"
      provides: "getLevelProgress() 查询函数"
      contains: "getLevelProgress"
    - path: "src/app/api/arena/progress/route.ts"
      provides: "GET /api/arena/progress 端点"
      exports: ["GET"]
    - path: "src/components/arena/LevelDetail.tsx"
      provides: "关卡详情页组件"
      exports: ["LevelDetail"]
  key_links:
    - from: "src/app/api/arena/progress/route.ts"
      to: "src/arena/db.ts"
      via: "getLevelProgress()"
      pattern: "getLevelProgress"
    - from: "src/components/arena/ArenaView.tsx"
      to: "/api/arena/progress"
      via: "refreshProgress authFetch"
      pattern: "authFetch.*api/arena/progress"
    - from: "src/components/arena/LevelDetail.tsx"
      to: "RunControls onStart"
      via: "callback prop delegation"
      pattern: "onStart"
---

<objective>
新增进度查询 API（`GET /api/arena/progress`）+ 关卡详情页组件（`LevelDetail`），实现顺序解锁逻辑。关卡详情页展示场景描述、评分维度说明（rubric items）、开始挑战按钮，并根据进度数据计算锁定/解锁状态。

Purpose: 实现 LEVL-04（顺序解锁）和 UI-05（关卡详情页），让用户能看到关卡的完整信息并按序挑战
Output: progress API 端点 + DB 查询 + LevelDetail 组件 + ArenaView 中 LevelDetail 替代 RunControls
</objective>

<execution_context>
@/home/user_demo/Husky/arena/.claude/get-shit-done/workflows/execute-plan.md
@/home/user_demo/Husky/arena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-world-navigation-progression/03-CONTEXT.md
@.planning/phases/03-world-navigation-progression/03-RESEARCH.md
@.planning/phases/03-world-navigation-progression/03-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的类型（执行者必须已有这些） -->

From src/components/arena/types.ts (Plan 01 扩展后):
```typescript
export interface ArenaLevelInfo {
  id: string;
  name: string;
  description: string;
  maxTurns: number;
  rubricCount: number;
}

export interface ArenaWorldInfo {
  id: string;
  name: string;
  description: string;
  icon?: string;
  levels: ArenaLevelInfo[];
}

export interface ArenaLevelWithProgress extends ArenaLevelInfo {
  cleared: boolean;
  locked: boolean;
}

export interface LevelProgress {
  cleared: boolean;
  runCount: number;
}

export interface SelectedLevel {
  worldId: string;
  level: ArenaLevelInfo;
}

export interface RunParams {
  worldId: string;
  levelId: string;
  gatekeeperProviderId?: string;
  gatekeeperModel?: string;
  challengerProviderId?: string;
  challengerModel?: string;
  graderProviderId?: string;
  graderModel?: string;
}
```

From src/arena/db.ts (现有 CRUD 模式):
```typescript
import { getDb } from '@/lib/db';
// 所有查询使用 prepared statements
// getDb() 返回 better-sqlite3 实例
// .prepare().all() 返回行数组
// .prepare().get() 返回单行或 undefined
```

From src/arena/types.ts:
```typescript
export interface ArenaRun {
  id: string;
  level_id: string;
  world_id: string;
  status: ArenaRunStatus; // 'running' | 'completed' | 'failed' | 'terminated'
  passed: number | null;
  // ...
}
```

From src/app/api/arena/levels/route.ts (API 路由模式):
```typescript
export const runtime = 'nodejs';
export const dynamic = 'force-dynamic';
export async function GET(request: NextRequest) {
  const authError = requireAuth(request);
  if (authError) return authError;
  try { ... return new Response(JSON.stringify(...), { status: 200, headers: { 'Content-Type': 'application/json' } });
  } catch { ... }
}
```

From src/arena/schemas/level-config.ts:
```typescript
export type RubricItem = {
  name: string;
  type: 'required' | 'performance';
  description: string;
  gradeDescriptions?: Record<string, string>;
};
```

From src/components/arena/RunControls.tsx:
```typescript
// RunControls 包含 RoleModelSelector 的三角色 provider/model 选择
// 保留 RunControls 的 provider 选择逻辑，LevelDetail 将内嵌 RunControls
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 getLevelProgress DB 查询 + GET /api/arena/progress API 端点</name>
  <files>
    src/arena/db.ts,
    src/app/api/arena/progress/route.ts
  </files>
  <read_first>
    src/arena/db.ts,
    src/arena/types.ts,
    src/app/api/arena/levels/route.ts,
    src/lib/auth.ts,
    src/lib/db.ts
  </read_first>
  <action>
1. **在 `src/arena/db.ts` 末尾新增 `getLevelProgress` 函数:**

```typescript
// ── Progress ──────────────────────────────────────────────────

/**
 * 查询所有关卡的通关进度。
 * 通关定义: 至少有一次 passed=1 的完成运行。
 * 返回以 level_id 为 key 的 Record。
 */
export function getLevelProgress(): Record<string, { cleared: boolean; runCount: number }> {
  const db = getDb();
  const rows = db.prepare(`
    SELECT level_id, MAX(passed) as has_cleared, COUNT(*) as run_count
    FROM arena_runs
    WHERE status IN ('completed', 'terminated')
    GROUP BY level_id
  `).all() as Array<{ level_id: string; has_cleared: number | null; run_count: number }>;

  const progress: Record<string, { cleared: boolean; runCount: number }> = {};
  for (const row of rows) {
    progress[row.level_id] = {
      cleared: row.has_cleared === 1,
      runCount: row.run_count,
    };
  }
  return progress;
}
```

2. **新建 `src/app/api/arena/progress/route.ts`:**

```typescript
import { NextRequest } from 'next/server';
import { requireAuth } from '@/lib/auth';
import { getLevelProgress } from '@/arena/db';

export const runtime = 'nodejs';
export const dynamic = 'force-dynamic';

/**
 * GET /api/arena/progress
 *
 * 返回所有关卡的通关进度。
 * Response: { progress: Record<string, { cleared: boolean; runCount: number }> }
 */
export async function GET(request: NextRequest) {
  const authError = requireAuth(request);
  if (authError) return authError;

  try {
    const progress = getLevelProgress();
    return new Response(JSON.stringify({ progress }), {
      status: 200,
      headers: { 'Content-Type': 'application/json' },
    });
  } catch (err) {
    return new Response(JSON.stringify({
      error: err instanceof Error ? err.message : 'Failed to load progress',
    }), {
      status: 500,
      headers: { 'Content-Type': 'application/json' },
    });
  }
}
```
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/arena && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/arena/db.ts 包含 `export function getLevelProgress()`
    - src/arena/db.ts 包含 `SELECT level_id, MAX(passed) as has_cleared, COUNT(*) as run_count`
    - src/arena/db.ts 包含 `WHERE status IN ('completed', 'terminated')`
    - src/app/api/arena/progress/route.ts 文件存在
    - src/app/api/arena/progress/route.ts 包含 `export async function GET`
    - src/app/api/arena/progress/route.ts 包含 `requireAuth(request)`
    - src/app/api/arena/progress/route.ts 包含 `getLevelProgress()`
    - src/app/api/arena/progress/route.ts 包含 `export const runtime = 'nodejs'`
    - tsc --noEmit 无类型错误
  </acceptance_criteria>
  <done>getLevelProgress 查询函数存在于 db.ts，GET /api/arena/progress 端点返回 { progress: Record } 结构，有 requireAuth 保护，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 新建 LevelDetail 组件 + 更新 ArenaView 右侧面板</name>
  <files>
    src/components/arena/LevelDetail.tsx,
    src/components/arena/ArenaView.tsx,
    src/components/arena/types.ts,
    src/i18n/en.ts,
    src/i18n/zh.ts
  </files>
  <read_first>
    src/components/arena/ArenaView.tsx,
    src/components/arena/RunControls.tsx,
    src/components/arena/types.ts,
    src/arena/schemas/level-config.ts,
    src/app/api/arena/levels/route.ts,
    src/i18n/en.ts,
    src/i18n/zh.ts
  </read_first>
  <action>
1. **扩展 types.ts — 新增 rubric 信息到 ArenaLevelInfo:**
   现有 `ArenaLevelInfo` 只有 `rubricCount`。为了在 LevelDetail 显示评分维度说明，需要扩展关卡详情获取方式。

   在 types.ts 新增:
   ```typescript
   /** 关卡详情（含完整 rubric 信息，用于详情页展示） */
   export interface ArenaLevelDetail extends ArenaLevelInfo {
     rubric: Array<{
       name: string;
       type: 'required' | 'performance';
       description: string;
     }>;
     challengerSystemPrompt?: string; // 不暴露给前端
   }
   ```

2. **修改 GET /api/arena/levels 返回 rubric 信息:**
   在 `src/app/api/arena/levels/route.ts` 的 levels mapping 中，在现有 `rubricCount` 之后追加 rubric 数组:
   ```typescript
   rubric: l.config.rubric.map(r => ({
     name: r.name,
     type: r.type,
     description: r.description,
   })),
   ```
   这样前端无需额外 API 调用即可获取 rubric 数据。

3. **新建 `src/components/arena/LevelDetail.tsx`:**

   'use client' 顶部声明。组件结构:

   Props 接口:
   ```typescript
   interface LevelDetailProps {
     level: ArenaLevelInfo;
     worldId: string;
     cleared: boolean;
     locked: boolean;
     progress: LevelProgress | undefined;
     onStart: (params: RunParams) => void;
     onBack: () => void;
   }
   ```

   布局（垂直滚动容器 `overflow-y-auto h-full`）:
   
   a) **顶部: 关卡标题区**
      - 返回按钮（Button variant="ghost"，ArrowLeft 图标 + `t('arena.backToLevels' as TranslationKey)`）
      - 关卡名称 h2 text-xl font-semibold
      - 关卡描述 text-sm text-muted-foreground
      - 如果 cleared: 显示 Badge variant="default" 内含 CheckCircle + `t('arena.detail.cleared' as TranslationKey)`
      - 如果 locked: 显示 Badge variant="secondary" 内含 Lock + `t('arena.detail.locked' as TranslationKey)`

   b) **中部: 评分维度说明 (UI-05)**
      - 标题: `t('arena.detail.rubricTitle' as TranslationKey)` = "Grading Criteria" / "评分标准"
      - 遍历 level 的 rubric 数组（从 `(level as ArenaLevelDetail).rubric`，如果存在）:
        - 每项: Badge 显示 type（required 用 destructive variant, performance 用 secondary variant）+ name + description
        - required 项的 Badge 文字: `t('arena.detail.required' as TranslationKey)` = "Required"/"必须"
        - performance 项的 Badge 文字: `t('arena.detail.performance' as TranslationKey)` = "Performance"/"表现"

   c) **底部: 运行控制**
      - 内嵌 `<RunControls>` 组件（复用现有 provider 选择逻辑）
      - 但是如果 locked: 运行控制区域显示锁定提示而非 RunControls
      - 锁定提示: `<div>` 内含 Lock 图标 + `t('arena.detail.unlockHint' as TranslationKey)` = "Clear the previous level to unlock" / "通关上一关即可解锁"

4. **更新 ArenaView 右侧面板:**
   在 ArenaView.tsx 的 renderMainContent 中:
   - 当 `viewState.phase === 'levels' && selectedLevel` 时，渲染 `<LevelDetail>` 而非 `<RunControls>`
   - 传入 cleared/locked/progress 从 ArenaView 的 progress state 获取:
     ```typescript
     const levelCleared = progress[selectedLevel.level.id]?.cleared ?? false;
     // 计算 locked: 找到该关卡在世界中的位置
     const world = worlds.find(w => w.id === selectedLevel.worldId);
     const levelIndex = world?.levels.findIndex(l => l.id === selectedLevel.level.id) ?? 0;
     const levelLocked = levelIndex > 0
       ? !(progress[world!.levels[levelIndex - 1].id]?.cleared ?? false)
       : false;
     ```
   - LevelDetail 的 onStart 回调与现有 handleStartRun 对接
   - LevelDetail 的 onBack 回调: setSelectedLevel(null)

5. **更新 i18n (en.ts + zh.ts):**
   追加以下 key:
   
   en.ts:
   ```
   'arena.detail.rubricTitle': 'Grading Criteria',
   'arena.detail.required': 'Required',
   'arena.detail.performance': 'Performance',
   'arena.detail.cleared': 'Cleared',
   'arena.detail.locked': 'Locked',
   'arena.detail.unlockHint': 'Clear the previous level to unlock this challenge',
   'arena.detail.maxTurns': 'Max {count} turns',
   'arena.detail.runCount': '{count} runs',
   ```
   
   zh.ts:
   ```
   'arena.detail.rubricTitle': '评分标准',
   'arena.detail.required': '必须',
   'arena.detail.performance': '表现',
   'arena.detail.cleared': '已通关',
   'arena.detail.locked': '未解锁',
   'arena.detail.unlockHint': '通关上一关即可解锁此挑战',
   'arena.detail.maxTurns': '最多 {count} 轮',
   'arena.detail.runCount': '{count} 次运行',
   ```

6. **同步修改 GET /api/arena/levels 端点**，在 levels mapping 中追加 rubric 数组（仅 name, type, description 三个字段，不暴露 gradeDescriptions 等内部数据）。
  </action>
  <verify>
    <automated>cd /home/user_demo/Husky/arena && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/arena/LevelDetail.tsx 包含 `export function LevelDetail`
    - src/components/arena/LevelDetail.tsx 包含 `rubricTitle` (评分维度标题)
    - src/components/arena/LevelDetail.tsx 包含 `unlockHint` (锁定提示)
    - src/components/arena/LevelDetail.tsx 包含 `onStart` prop
    - src/components/arena/ArenaView.tsx 包含 `import { LevelDetail }`
    - src/components/arena/ArenaView.tsx 包含 `LevelDetail` 渲染
    - src/components/arena/types.ts 包含 `export interface ArenaLevelDetail`
    - src/app/api/arena/levels/route.ts 包含 `rubric: l.config.rubric.map`
    - src/i18n/en.ts 包含 `'arena.detail.rubricTitle': 'Grading Criteria'`
    - src/i18n/zh.ts 包含 `'arena.detail.rubricTitle': '评分标准'`
    - tsc --noEmit 无类型错误
  </acceptance_criteria>
  <done>LevelDetail 组件渲染关卡完整信息（标题、描述、评分标准、锁定状态、开始按钮），ArenaView 右侧面板使用 LevelDetail 替代直接的 RunControls，顺序解锁逻辑生效，i18n 双语同步，TypeScript 编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> GET /api/arena/progress | 新端点，返回进度聚合数据 |
| client -> GET /api/arena/levels | 现有端点，新增 rubric 字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | /api/arena/progress | mitigate | requireAuth 保护端点，仅返回 level_id/cleared/runCount，不包含运行内容 |
| T-03-04 | Information Disclosure | /api/arena/levels rubric | accept | rubric 信息是面向用户的评分说明，非敏感数据。仅暴露 name/type/description，不暴露 systemPrompt |
| T-03-05 | Injection | getLevelProgress SQL | mitigate | 查询无用户输入参数（聚合全表），prepared statement 模式与现有 db.ts 一致 |
| T-03-06 | Elevation of Privilege | 解锁校验绕过 | accept | 解锁逻辑为前端 UX 引导，单用户桌面应用无需后端强制校验（per Research A1） |
</threat_model>

<verification>
1. `npx tsc --noEmit` 无错误
2. /api/arena/progress 端点返回 `{ progress: { "level-id": { cleared: true/false, runCount: N } } }`
3. LevelDetail 组件正确显示关卡名称、描述、评分标准
4. 锁定关卡不可开始运行
5. i18n en.ts/zh.ts 包含所有 arena.detail.* key
</verification>

<success_criteria>
- GET /api/arena/progress 可正常返回进度数据
- 关卡详情页显示场景描述、评分维度说明（required/performance 分类标记）
- 锁定关卡显示 "通关上一关即可解锁" 提示，开始按钮不可用
- 每世界首关默认解锁
- 已通关关卡可重复挑战
- TypeScript 编译通过，i18n 双语同步
</success_criteria>

<output>
After completion, create `.planning/phases/03-world-navigation-progression/03-02-SUMMARY.md`
</output>
