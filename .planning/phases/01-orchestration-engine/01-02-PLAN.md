---
phase: 01-orchestration-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arena/schemas/level-config.ts
  - src/arena/schemas/grader-output.ts
  - src/arena/schemas/gatekeeper-output.ts
  - src/arena/level-loader.ts
  - src/arena/levels/customer-service/world.json
  - src/arena/levels/customer-service/level-01.json
  - src/arena/levels/customer-service/level-02.json
  - src/__tests__/unit/arena-level-loader.test.ts
autonomous: true
requirements:
  - LEVL-01
  - LEVL-02
  - INTG-02
  - GRAD-02
  - GRAD-03

must_haves:
  truths:
    - "Level JSON files are validated by Zod schema at load time, invalid configs produce clear error messages"
    - "Level loader discovers and loads all worlds and levels from src/arena/levels/ directory"
    - "Each level config includes rubric with required (Pass/Fail) and performance (A/B/C/D) items with grade descriptions"
    - "Three roles (Gatekeeper, Challenger, Grader) can each have independent provider/model config in level JSON"
    - "Example customer-service world has 2 progressive levels with complete rubrics"
  artifacts:
    - path: "src/arena/schemas/level-config.ts"
      provides: "Zod schemas for LevelConfig and WorldConfig with rubric items"
      exports: ["LevelConfigSchema", "WorldConfigSchema", "RubricItemSchema", "LevelConfig", "WorldConfig", "RubricItem"]
    - path: "src/arena/schemas/grader-output.ts"
      provides: "Zod schema for Grader structured output (GRAD-02, GRAD-04, GRAD-05)"
      exports: ["GraderOutputSchema", "GraderOutput"]
    - path: "src/arena/schemas/gatekeeper-output.ts"
      provides: "Zod schema for Gatekeeper structured output with shouldEnd"
      exports: ["GatekeeperOutputSchema", "GatekeeperOutput"]
    - path: "src/arena/level-loader.ts"
      provides: "Level discovery, loading, and validation functions"
      exports: ["loadAllWorlds", "loadLevel", "getWorldLevels", "LoadedWorld", "LoadedLevel"]
    - path: "src/arena/levels/customer-service/world.json"
      provides: "Example world metadata"
    - path: "src/arena/levels/customer-service/level-01.json"
      provides: "First example level (basic greeting scenario)"
    - path: "src/arena/levels/customer-service/level-02.json"
      provides: "Second example level (complaint handling scenario)"
  key_links:
    - from: "src/arena/level-loader.ts"
      to: "src/arena/schemas/level-config.ts"
      via: "Zod safeParse validation"
      pattern: "LevelConfigSchema\\.safeParse|WorldConfigSchema\\.safeParse"
    - from: "src/arena/level-loader.ts"
      to: "src/arena/levels/"
      via: "fs.readdirSync + JSON.parse"
      pattern: "readdirSync.*levels"
---

<objective>
Arena 关卡体系：定义 Zod schemas（关卡配置、Grader 输出、Gatekeeper 输出），实现关卡加载器，编写示例世界内容。

Purpose: 为编排引擎提供关卡配置基础设施，包括评分标准（rubric anchoring）和角色级 provider/model 配置。Grader 和 Gatekeeper 的输出 schema 也在此定义，供引擎模块使用。
Output: Zod schema 文件、level-loader、1 个示例世界含 2 个关卡、单元测试。
</objective>

<execution_context>
@/home/user_demo/Husky/arena/.claude/get-shit-done/workflows/execute-plan.md
@/home/user_demo/Husky/arena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-orchestration-engine/01-CONTEXT.md
@.planning/phases/01-orchestration-engine/01-RESEARCH.md

<interfaces>
<!-- Zod v4 is available (4.3.6), AI SDK v6.0.73 peerDep supports zod ^4.1.8 -->
<!-- Project uses import { z } from 'zod' -->
<!-- Level files bundled at src/arena/levels/ per D-04 -->
<!-- World-folder grouping per D-05: world.json + level-XX.json per world dir -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod Schemas -- 关卡配置、Grader 输出、Gatekeeper 输出 (LEVL-01, GRAD-02, GRAD-03, GRAD-04, GRAD-05, INTG-02)</name>
  <files>src/arena/schemas/level-config.ts, src/arena/schemas/grader-output.ts, src/arena/schemas/gatekeeper-output.ts</files>
  <read_first>src/lib/text-generator.ts, src/types/index.ts</read_first>
  <behavior>
    - Test 1: Valid LevelConfig JSON passes LevelConfigSchema.safeParse with success=true
    - Test 2: Invalid LevelConfig (missing id) fails safeParse with descriptive error
    - Test 3: WorldConfigSchema validates world.json structure
    - Test 4: GraderOutputSchema validates mixed scoring output (required Pass/Fail + performance A/B/C/D)
    - Test 5: GraderOutputSchema rejects suggestions array with more than 3 items
    - Test 6: GatekeeperOutputSchema validates shouldEnd boolean field
    - Test 7: RubricItem with type='performance' requires gradeDescriptions with A/B/C/D keys
  </behavior>
  <action>
1. 创建 `src/arena/schemas/level-config.ts`（per LEVL-01 关卡 JSON 定义、per INTG-02 三角色独立 provider/model、per GRAD-03 rubric anchoring）：

```typescript
import { z } from 'zod';

export const RubricItemSchema = z.object({
  name: z.string().describe('评分项名称'),
  type: z.enum(['required', 'performance']).describe('required=必须项 Pass/Fail, performance=表现项 A/B/C/D'),
  description: z.string().describe('评分项说明'),
  gradeDescriptions: z.record(z.string()).optional().describe('等级行为描述 mapping，required 类型不需要，performance 类型应包含 A/B/C/D 键'),
});

export const LevelConfigSchema = z.object({
  id: z.string(),
  name: z.string(),
  description: z.string().describe('场景描述（展示给用户）'),
  challengerSystemPrompt: z.string().describe('Challenger 的 system prompt'),
  gatekeeperSystemPrompt: z.string().describe('Gatekeeper 的 system prompt'),
  graderSystemPrompt: z.string().optional().describe('Grader 的 system prompt（可选，有默认值）'),
  rubric: z.array(RubricItemSchema).min(1).describe('评分标准，至少 1 项'),
  maxTurns: z.number().int().positive().default(10).describe('最大对话轮数'),
  maxTokens: z.number().int().positive().optional().describe('Token 预算上限（覆盖默认 200,000）'),
  roleConfig: z.object({
    gatekeeper: z.object({ providerId: z.string().optional(), model: z.string().optional() }).optional(),
    challenger: z.object({ providerId: z.string().optional(), model: z.string().optional() }).optional(),
    grader: z.object({ providerId: z.string().optional(), model: z.string().optional() }).optional(),
  }).optional().describe('角色级别的 provider/model 覆盖'),
  sortOrder: z.number().int().default(0).describe('关卡在世界内的排序'),
});

export const WorldConfigSchema = z.object({
  id: z.string(),
  name: z.string(),
  description: z.string(),
  icon: z.string().optional(),
  sortOrder: z.number().int().default(0),
});

export type LevelConfig = z.infer<typeof LevelConfigSchema>;
export type WorldConfig = z.infer<typeof WorldConfigSchema>;
export type RubricItem = z.infer<typeof RubricItemSchema>;
```

2. 创建 `src/arena/schemas/grader-output.ts`（per GRAD-02 混合评分制、per GRAD-04 Zod 校验、per GRAD-05 最多 3 条建议引用轮次）：

```typescript
import { z } from 'zod';

export const GraderOutputSchema = z.object({
  passed: z.boolean().describe('是否通关（所有必须项全部 Pass 才为 true）'),
  requiredCriteria: z.array(z.object({
    name: z.string().describe('必须项名称（对应 rubric 中 type=required 的项）'),
    passed: z.boolean().describe('是否通过'),
    reason: z.string().describe('判定理由'),
  })).describe('必须项评估结果'),
  performanceDimensions: z.array(z.object({
    name: z.string().describe('表现维度名称（对应 rubric 中 type=performance 的项）'),
    grade: z.enum(['A', 'B', 'C', 'D']).describe('等级评分'),
    reason: z.string().describe('判定理由'),
  })).describe('表现项评估结果'),
  suggestions: z.array(z.object({
    content: z.string().describe('改进建议内容'),
    referenceTurn: z.number().int().min(0).describe('引用的对话轮次编号（从 0 开始）'),
  })).max(3).describe('最多 3 条改进建议，须引用具体对话轮次'),
});

export type GraderOutput = z.infer<typeof GraderOutputSchema>;
```

3. 创建 `src/arena/schemas/gatekeeper-output.ts`（per ORCH-02 shouldEnd 结构化输出）：

```typescript
import { z } from 'zod';

export const GatekeeperOutputSchema = z.object({
  message: z.string().describe('Gatekeeper 的提问内容或结束语'),
  shouldEnd: z.boolean().describe('是否结束对话'),
  endReason: z.string().optional().describe('结束原因（仅 shouldEnd=true 时填写）'),
});

export type GatekeeperOutput = z.infer<typeof GatekeeperOutputSchema>;
```
  </action>
  <verify>
    <automated>npx tsx --test src/__tests__/unit/arena-level-loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/arena/schemas/level-config.ts 存在且导出 `LevelConfigSchema`、`WorldConfigSchema`、`RubricItemSchema`、`LevelConfig`、`WorldConfig`、`RubricItem`
    - src/arena/schemas/grader-output.ts 存在且导出 `GraderOutputSchema`、`GraderOutput`
    - src/arena/schemas/gatekeeper-output.ts 存在且导出 `GatekeeperOutputSchema`、`GatekeeperOutput`
    - GraderOutputSchema 包含 `suggestions` 字段且带 `.max(3)` 限制
    - GraderOutputSchema 包含 `requiredCriteria` 数组（必须项 Pass/Fail）和 `performanceDimensions` 数组（表现项 A/B/C/D）
    - LevelConfigSchema 包含 `roleConfig` 字段允许三角色独立配置 provider/model
    - GatekeeperOutputSchema 包含 `shouldEnd: z.boolean()`
  </acceptance_criteria>
  <done>三个 Zod schema 文件完整定义：关卡配置支持 rubric anchoring 和角色级 provider/model 覆盖，Grader 输出支持混合评分制和改进建议，Gatekeeper 输出支持 shouldEnd 控制</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 关卡加载器 + 示例世界内容 (LEVL-02, D-04, D-05, D-06)</name>
  <files>src/arena/level-loader.ts, src/arena/levels/customer-service/world.json, src/arena/levels/customer-service/level-01.json, src/arena/levels/customer-service/level-02.json, src/__tests__/unit/arena-level-loader.test.ts</files>
  <read_first>src/arena/schemas/level-config.ts</read_first>
  <behavior>
    - Test 1: loadAllWorlds discovers customer-service world and returns it with 2 levels
    - Test 2: loadLevel returns a validated LevelConfig for a valid level file
    - Test 3: loadAllWorlds rejects an invalid level file with clear error message containing the field name
    - Test 4: getWorldLevels returns levels sorted by sortOrder
    - Test 5: loadAllWorlds ignores non-directory entries in levels/
  </behavior>
  <action>
1. 创建 `src/arena/levels/customer-service/world.json`（per D-05 世界文件夹分组、per D-06 示例世界）：

```json
{
  "id": "customer-service",
  "name": "Customer Service Agent",
  "description": "Test an AI agent's ability to handle customer service scenarios with empathy, accuracy, and professionalism.",
  "icon": "headset",
  "sortOrder": 0
}
```

2. 创建 `src/arena/levels/customer-service/level-01.json`（per D-06 第一个渐进关卡 -- 基础问候）：

```json
{
  "id": "cs-greeting",
  "name": "Basic Greeting & Identification",
  "description": "The agent must properly greet the customer, identify themselves, and ask how they can help. Tests fundamental customer service etiquette.",
  "challengerSystemPrompt": "You are a customer service AI agent for TechCorp, a consumer electronics company. Greet customers warmly, identify yourself by name (Alex), and ask how you can assist them today. Be professional, friendly, and concise.",
  "gatekeeperSystemPrompt": "You are testing a customer service AI agent. Play the role of a customer calling about a general product inquiry. Start by saying hello and see if the agent greets you properly, introduces themselves, and asks how they can help. After 2-3 exchanges, end the conversation. Evaluate whether the agent followed basic greeting protocols.",
  "rubric": [
    {
      "name": "Self-Introduction",
      "type": "required",
      "description": "Agent must introduce themselves by name and company within the first response"
    },
    {
      "name": "Greeting Quality",
      "type": "required",
      "description": "Agent must use a warm, professional greeting (not robotic or overly casual)"
    },
    {
      "name": "Helpfulness Offer",
      "type": "required",
      "description": "Agent must explicitly ask how they can help the customer"
    },
    {
      "name": "Tone & Professionalism",
      "type": "performance",
      "description": "Overall tone and professionalism of the interaction",
      "gradeDescriptions": {
        "A": "Exceptionally warm and natural tone, feels like talking to a skilled human agent",
        "B": "Professional and friendly, appropriate for customer service",
        "C": "Adequate but somewhat robotic or generic",
        "D": "Cold, overly formal, or inappropriately casual"
      }
    }
  ],
  "maxTurns": 6,
  "sortOrder": 0
}
```

3. 创建 `src/arena/levels/customer-service/level-02.json`（per D-06 第二个渐进关卡 -- 投诉处理）：

```json
{
  "id": "cs-complaint",
  "name": "Complaint Handling",
  "description": "The agent must handle an upset customer complaining about a defective product. Tests empathy, problem-solving, and de-escalation skills.",
  "challengerSystemPrompt": "You are a customer service AI agent for TechCorp. A customer is upset about a defective product. Listen empathetically, acknowledge their frustration, apologize sincerely, and offer a concrete solution (replacement, refund, or repair). Follow the company policy: products within 30 days get full refund or replacement; 30-90 days get repair or store credit.",
  "gatekeeperSystemPrompt": "You are testing a customer service AI agent. Play the role of an angry customer who bought a wireless headphone 2 weeks ago that stopped working. Express frustration and see how the agent handles it. Escalate your tone if the agent doesn't show empathy. After 4-5 exchanges, end the conversation.",
  "rubric": [
    {
      "name": "Empathy Acknowledgment",
      "type": "required",
      "description": "Agent must acknowledge the customer's frustration and apologize within the first 2 responses"
    },
    {
      "name": "Solution Offered",
      "type": "required",
      "description": "Agent must offer a concrete resolution (refund, replacement, or repair) consistent with the 30-day policy"
    },
    {
      "name": "No Blame",
      "type": "required",
      "description": "Agent must not blame the customer or imply the issue is their fault"
    },
    {
      "name": "De-escalation Skill",
      "type": "performance",
      "description": "Ability to calm down the upset customer",
      "gradeDescriptions": {
        "A": "Customer's tone noticeably shifts from angry to satisfied through the conversation",
        "B": "Agent remains calm and professional, customer's frustration doesn't escalate",
        "C": "Agent handles the situation but doesn't actively de-escalate",
        "D": "Agent's responses could further upset the customer"
      }
    },
    {
      "name": "Policy Accuracy",
      "type": "performance",
      "description": "Correctness of policy information provided",
      "gradeDescriptions": {
        "A": "Cites specific policy details accurately (30-day window, options available)",
        "B": "Generally correct policy information without specific details",
        "C": "Vague about policy, but doesn't provide wrong information",
        "D": "Provides incorrect policy information or makes promises outside policy"
      }
    }
  ],
  "maxTurns": 10,
  "sortOrder": 1
}
```

4. 创建 `src/arena/level-loader.ts`（per LEVL-02 启动时 Zod 校验、per D-04 bundled in codebase）：

```typescript
import fs from 'fs';
import path from 'path';
import { LevelConfigSchema, WorldConfigSchema } from './schemas/level-config';
import type { LevelConfig, WorldConfig } from './schemas/level-config';

export interface LoadedLevel {
  config: LevelConfig;
  worldId: string;
}

export interface LoadedWorld {
  config: WorldConfig;
  levels: LoadedLevel[];
}

// 关卡文件目录（bundled in codebase per D-04）
const LEVELS_DIR = path.join(__dirname, 'levels');

/**
 * 加载并校验所有世界和关卡配置。
 * 启动时调用（per LEVL-02），无效配置抛出含具体字段名的错误。
 */
export function loadAllWorlds(): LoadedWorld[] {
  if (!fs.existsSync(LEVELS_DIR)) {
    console.warn('[arena] Levels directory not found:', LEVELS_DIR);
    return [];
  }

  const entries = fs.readdirSync(LEVELS_DIR, { withFileTypes: true });
  const worlds: LoadedWorld[] = [];

  for (const entry of entries) {
    if (!entry.isDirectory()) continue;

    const worldDir = path.join(LEVELS_DIR, entry.name);
    const worldJsonPath = path.join(worldDir, 'world.json');

    if (!fs.existsSync(worldJsonPath)) {
      console.warn(`[arena] Skipping directory without world.json: ${entry.name}`);
      continue;
    }

    // 加载 world.json
    const worldRaw = JSON.parse(fs.readFileSync(worldJsonPath, 'utf-8'));
    const worldResult = WorldConfigSchema.safeParse(worldRaw);
    if (!worldResult.success) {
      throw new Error(`[arena] Invalid world.json in ${entry.name}: ${worldResult.error.message}`);
    }

    // 加载该世界下的所有 level-*.json
    const levelFiles = fs.readdirSync(worldDir)
      .filter(f => f.startsWith('level-') && f.endsWith('.json'))
      .sort();

    const levels: LoadedLevel[] = [];
    for (const levelFile of levelFiles) {
      const levelPath = path.join(worldDir, levelFile);
      const levelRaw = JSON.parse(fs.readFileSync(levelPath, 'utf-8'));
      const levelResult = LevelConfigSchema.safeParse(levelRaw);
      if (!levelResult.success) {
        throw new Error(`[arena] Invalid level config ${entry.name}/${levelFile}: ${levelResult.error.message}`);
      }
      levels.push({ config: levelResult.data, worldId: worldResult.data.id });
    }

    // 按 sortOrder 排序
    levels.sort((a, b) => a.config.sortOrder - b.config.sortOrder);

    worlds.push({ config: worldResult.data, levels });
  }

  // 按 sortOrder 排序世界
  worlds.sort((a, b) => a.config.sortOrder - b.config.sortOrder);
  return worlds;
}

/**
 * 加载单个关卡配置（按 worldId + levelId 查找）
 */
export function loadLevel(worldId: string, levelId: string): LoadedLevel | null {
  const worlds = loadAllWorlds();
  for (const world of worlds) {
    if (world.config.id === worldId) {
      const level = world.levels.find(l => l.config.id === levelId);
      return level || null;
    }
  }
  return null;
}

/**
 * 获取指定世界的所有关卡（按 sortOrder 排序）
 */
export function getWorldLevels(worldId: string): LoadedLevel[] {
  const worlds = loadAllWorlds();
  const world = worlds.find(w => w.config.id === worldId);
  return world?.levels || [];
}
```

5. 创建测试文件 `src/__tests__/unit/arena-level-loader.test.ts`，使用 `node:test` + `node:assert/strict`。测试 loadAllWorlds 能发现示例世界、loadLevel 返回合法 LevelConfig、无效配置抛出含字段名的错误。需要 mock 或使用实际的 levels 目录。
  </action>
  <verify>
    <automated>npx tsx --test src/__tests__/unit/arena-level-loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/arena/schemas/level-config.ts 导出 LevelConfigSchema 和 WorldConfigSchema
    - src/arena/schemas/grader-output.ts 导出 GraderOutputSchema 且包含 `.max(3)` 和 `referenceTurn`
    - src/arena/schemas/gatekeeper-output.ts 导出 GatekeeperOutputSchema 且包含 `shouldEnd: z.boolean()`
    - src/arena/level-loader.ts 导出 loadAllWorlds、loadLevel、getWorldLevels
    - src/arena/levels/customer-service/world.json 存在且包含 `"id": "customer-service"`
    - src/arena/levels/customer-service/level-01.json 存在且包含 `"id": "cs-greeting"` 和至少 3 个 `"type": "required"` rubric 项
    - src/arena/levels/customer-service/level-02.json 存在且包含 `"id": "cs-complaint"` 和至少 2 个 `"type": "performance"` rubric 项带 `gradeDescriptions`
    - src/__tests__/unit/arena-level-loader.test.ts 执行退出码为 0
    - `npm run test` 通过
  </acceptance_criteria>
  <done>关卡配置 Zod schema 完整定义（含 rubric anchoring），关卡加载器能发现并校验所有世界和关卡，示例世界含 2 个渐进关卡，单元测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON files -> Zod parse | Level JSON 内容经 Zod 校验后才被信任使用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | src/arena/level-loader.ts | mitigate | 所有 JSON 文件经 Zod safeParse 校验，无效配置抛出错误而非静默使用 |
| T-01-02 | Injection | src/arena/levels/*.json | accept | Level 文件 bundled in codebase（D-04），非用户可编辑的外部输入；无需额外防护 |
| T-01-03 | Information Disclosure | src/arena/schemas/*.ts | accept | Schema 定义无敏感信息，仅定义数据结构 |
</threat_model>

<verification>
1. `npx tsx --test src/__tests__/unit/arena-level-loader.test.ts` 通过
2. `npm run test` 通过（typecheck + 所有单元测试）
3. `node -e "const l = require('./src/arena/level-loader'); console.log(l.loadAllWorlds())"` 输出含 customer-service 世界（或等效的 tsx 执行）
4. `grep -r "safeParse" src/arena/level-loader.ts` 确认使用 Zod 校验
</verification>

<success_criteria>
- 三个 Zod schema 文件完整定义且类型导出
- 关卡加载器能发现 src/arena/levels/ 下所有世界目录并校验配置
- 无效配置产生含具体字段名的清晰错误信息
- 示例世界含 2 个关卡，第一关 3 个必须项 + 1 个表现项，第二关 3 个必须项 + 2 个表现项
- 所有表现项包含 A/B/C/D gradeDescriptions（rubric anchoring）
- 单元测试全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/01-orchestration-engine/01-02-SUMMARY.md`
</output>
